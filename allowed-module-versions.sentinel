import "tfplan/v2" as tfplan

# Define the main rule
main = rule {
  all tfplan.module_paths as module_path {
    all tfplan.module(module_path).resources as resource {
      resource.type is "module" and
      resource.name is "ssm1" and
      resource.version is "1.0.0"
    }
  }
}
